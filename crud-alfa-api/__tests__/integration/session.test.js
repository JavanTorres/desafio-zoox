
describe("Authentication", () => {
    it("Precisa ser a soma de dois números", () => {
        const x = 1;
        const y = 2;
        const sum = x + y;

        expect(sum).toBe(3);
        // expect(sum).toBe(4);
    });



});
